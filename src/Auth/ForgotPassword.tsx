import React from "react"
import styled from "styled-components"
import "./Auth.module.css"

const HomeWrapper = styled.div`
  height: 100%;
  width: 100%;
  padding: 10px;
`

export const ForgotPassword: React.FC = (props) => {
  return <HomeWrapper>ForgotPassword</HomeWrapper>
}
